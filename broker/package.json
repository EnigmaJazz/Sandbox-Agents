{
  "name": "opencode-sandbox-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Trusted sandbox broker: policy, worker lifecycle, git snapshot/result boundary (SYSTEM_PROMPT.md §6-§22). Gate 1: code only, nothing installed.",
  "scripts": {
    "test": "bun test",
    "dev": "bun --watch src/main.ts",
    "start": "bun src/main.ts"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
